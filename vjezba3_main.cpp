#include "vjezba3.hpp"
#include <vector>
#include<iostream>
using namespace std;


int main()
{
	vector<int> nesto;
	vector<int> prvi;
	vector<int> drugi;
	vector<int> rezultat;
	/*unos1(nesto, 5);
	ispis(nesto);
	vector.clear();
	unos2(nesto, 2, 14);
	ispis(nesto);*/

	unos1(prvi, 7);
	ispis(prvi);
	unos1(drugi, 5);
	ispis(drugi);
}